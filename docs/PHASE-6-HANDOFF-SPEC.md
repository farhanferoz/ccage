# Phase 6 — Handoff + Resume Cost Interception (Spec v2)

Workflow: **Sonnet implements each subphase TDD-style; Opus reviews when subphase reports green.** Same rhythm as PLAN.md.

## Why

`claude --resume` (`-r`) and `claude --continue` (`-c`) trigger a **structural** prompt-cache miss in Claude Code, not a TTL-based one: `processSessionStartHooks('resume')` and `reorderAttachmentsForAPI` shuffle bytes at the start of `messages`, breaking cache at offset 0 every time. Cache continuity is **never preserved** across resume — verified by GitHub issues #42309, #43657, #44045, #51764 across versions 2.1.69–2.1.138, and by empirical inspection of session JSONLs on this machine (2-minute gap still produced a 36K-token cache-write spike).

Users pay the cache-write tax on every resume. On a 100K-token Opus session: ~$1.88 per resume. There is no Claude-Code-side knob to fix this; cache TTL is also not user-configurable (no settings.json key, env var, or CLI flag exposes the 1h-extended ttl).

ccage's role: surface the cost before launch, and offer a free, offline alternative — a structural handoff brief extracted from the session JSONL with `jq`.

## Goals

- **A.** `ccage handoff [<session-id>]` — produce a Markdown brief from a session JSONL using only `jq` and shell. Zero API calls. Output suitable for pasting as the first message of a fresh `claude` session.
- **B.** `-r` / `-c` cost interception in the `claude` shell function — compute precise rewrite cost from JSONL `usage` fields and prompt user before launch.

## Non-goals

- LLM-quality narrative synthesis (`/session-handoff` from inside a warm session already covers this — users invoke it manually).
- Daemonized idle detection or keep-alive ping.
- Fixing Claude Code's structural cache miss (Anthropic's problem).
- Auto-paste / auto-clear (too magical; brittle).

---

## Feature A — `ccage handoff`

### Surface

```
ccage handoff                          # most-recent session for $PWD
ccage handoff <session-id>             # specific session (UUID, prefix-match allowed)
ccage handoff --output FILE            # explicit path; default derived from $CCAGE_HANDOFF_DIR
ccage handoff --project <abs-path>     # other project's session
ccage handoff --max-prompts N          # cap user-prompt section (default 20)
ccage handoff --stdout                 # write to stdout instead of file
ccage handoff --config-dir DIR         # read sessions from cage DIR      (added 2026-07-20)
ccage handoff --no-subagents           # skip delegated work              (added 2026-07-20)
```

**Default output directory:** `${CCAGE_HANDOFF_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/ccage/handoffs}`. Global flat layout — one directory across all projects. Filenames include project slug to disambiguate: `<project-slug>-<session-prefix>-<YYYYMMDD-HHMMSS>.md`.

No `--json` flag. YAGNI; add when a consumer exists.

### Lookup

1. Project slug derived from `$PWD` (or `--project`) by `${path//\//-}`. Matches Claude Code's internal naming — verified empirically against `~/.claude/projects/`.
2. Session directory: `<cage>/projects/<slug>/`. **Amended 2026-07-20** — this
   spec assumed `$CLAUDE_CONFIG_DIR` would be set, but `ccage` runs from a plain
   shell and the `claude()` wrapper exports it with `local -x` so it never leaks
   out. The cage is resolved: `--config-dir` → `$CLAUDE_CONFIG_DIR` →
   `_ccage_config_dir_for` (sourced from `claude-isolation.sh`, so `CCAGE_ROOT`/
   `CCAGE_PREFIX`/`CCAGE_SLOT`/`_ccage_config_dir_override` all apply) → a scan
   of every cage for an `.owning_path` naming the project, newest session
   winning when several cages own one path.
3. No `<session-id>` arg → most-recent by mtime (`ls -t "$DIR"/*.jsonl | head -1`).
4. Prefix `<session-id>` → match by leading chars; multiple matches → exit 2 with candidate list.

### Brief format

```markdown
# Handoff: <session-id>

**Project:** /home/ff235/dev/ccage
**Started:** 2026-05-15 09:14
**Last activity:** 2026-05-16 18:32 (4h 12m ago)
**Turns:** 47 user / 47 assistant
**Tokens billed so far:** 124,567 input · 8,432 output · 89,034 cache-write · 412,890 cache-read
**Estimated cost so far:** ~$2.13 (claude-opus-4-7, 200K tier)
**Last model used:** claude-opus-4-7

## User prompts (last 20 of 47)

1. ...
2. ...
...
20. ...

_(27 earlier prompts elided — see raw JSONL at <path> for full history)_

## Files touched (top 30 by frequency)

| Path | Read | Edit | Write |
|---|---:|---:|---:|
| /home/ff235/dev/ccage/RESUME.md | 3 | 1 | 0 |
| ... |

## Commands run (deduplicated, trivial elided)

- `git log --oneline -20`
- ...

## Last assistant turn

<verbatim text; truncated to first+last 300 words if >600 words, marked with `…(N words elided)…`>

---
Generated by `ccage handoff` from <jsonl-path> on 2026-05-16T22:30:00Z
```

### Truncation defaults

- User prompts: most-recent 20 (older counted in elision marker).
- Files-touched table: top 30 rows by `Read+Edit+Write` count.
- Bash commands: deduplicate exact-match; drop trivial (`pwd`, `ls`, `true`, `clear`). Cap at 40 unique entries.
- Last assistant turn: if >600 words, keep first 300 + last 300, insert `…(N words elided)…` between.

All thresholds tunable via env vars in a later phase if real-world use shows they're wrong.

### jq extraction primitives (`share/ccage-handoff.sh`)

| Datum | jq approach |
|---|---|
| User prompts (human only) | `select(.type=="user") \| select(.toolUseResult==null) \| select(.isMeta!=true) \| .message.content \| (string-or-text-array)` |
| Files touched | `select(.type=="assistant") \| .message.content[]? \| select(.type=="tool_use" and (.name=="Read" or .name=="Edit" or .name=="Write")) \| {op:.name, path:(.input.file_path // .input.path)}` → group/count |
| Bash commands | `select(.type=="assistant") \| .message.content[]? \| select(.type=="tool_use" and .name=="Bash") \| .input.command` → dedup, filter trivials |
| Last assistant text | `select(.type=="assistant") \| .message.content[]? \| select(.type=="text") \| .text` → `tail -n 1` |
| Token totals | sum across all assistant turns' `.message.usage.*` |
| Last model | last assistant record's `.message.model` |
| Timestamps | first/last record `.timestamp` |

Implementation must include test fixtures covering:
- `<system-reminder>`-wrapped synthetic user messages (exclude).
- Tool-result records (exclude from "user prompts").
- Multi-line content arrays vs string content (both render).
- Empty/malformed lines (skip, count, warn at end).

### Edge cases

- No JSONL exists → exit 2, message: `no sessions found for $PWD`.
- Multiple matches on prefix → exit 2, list candidates one per line.
- Malformed JSONL line → skip, increment warning count, print summary at end. Log full jq error to `$CCAGE_DIR/handoff.log`.
- Zero user prompts → still emit brief with placeholder section.

### Entry point

New `bin/ccage` POSIX-sh dispatcher (matches the `ccage list/doctor/prune` roadmap in RESUME.md):

```sh
case "$1" in
  handoff) shift; exec "$CCAGE_LIB/ccage-handoff.sh" "$@" ;;
  -h|--help) ... ;;
  *) echo "ccage: unknown command: $1" >&2; exit 2 ;;
esac
```

Installer drops `bin/ccage` into `$PREFIX/bin/` and verifies it's on PATH.

### "Next steps" UX after generation

Default behavior (when invoked from interactive shell):
1. Write brief to `${CCAGE_HANDOFF_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/ccage/handoffs}/<project-slug>-<session-prefix>-<YYYYMMDD-HHMMSS>.md`.
2. Print path on stdout.
3. Print next-step hint: `next: run \`claude\` (fresh session) and paste this file's contents as your first message`.
4. Clipboard auto-copy if available — detection order: `pbcopy` (macOS) → `wl-copy` (Wayland) → `xclip` (X11) → `xsel` (X11 fallback). First match wins. Append `(also copied to clipboard)` to the hint when successful. Skip silently if none found. No OSC 52 escape fallback in v0 (some terminals choke on it).

With `--stdout`: print brief to stdout, no path, no hint, no clipboard. For piping/scripts.

---

## Feature B — `-r` / `-c` Cost Interception

### Where it hooks

`share/claude-isolation.sh`, inside the `claude()` shell function, between `_ccage_bootstrap_dir` (current line 197) and `_ccage_pre_exec_hook` (current line 204):

```bash
_ccage_intercept_resume "$@" || return $?
```

### Detection — three regimes

The bare-args case has to be handled differently from explicit-id, because `claude -r` without a session id may invoke Claude Code's own interactive session picker (we can't predict which session the user picks).

| Args | Behavior | Why |
|---|---|---|
| Contains `-c` or `--continue` | Intercept. Use most-recent session by mtime. | Deterministic — Claude Code always picks most-recent. |
| Contains `--resume <id>` or `-r <id>` (next arg looks like UUID/prefix) | Intercept. Use that session id. | Deterministic — user named the session. |
| Contains bare `-r` / `--resume` with no id (or next arg is another flag) | **Pass through, no prompt.** | Claude Code may show its own picker; we don't know which one will be picked. |

Flag-vs-positional distinction: treat the token after `-r`/`--resume` as a session id if and only if it matches `^[0-9a-f-]{6,}$` (UUID prefix shape).

### Cost computation

The empirical evidence from JSONLs shows:
- Tools+system prefix (~19K typical) hits at account level even on resume.
- The message prefix (everything else cached) gets fully re-written.
- Pre-gap `cache_read` value on the last substantive assistant turn ≈ size of cached prefix at that moment.

Formula:

```
substantive_reads = [cache_read_input_tokens of each assistant turn where it exceeds 1000]
if substantive_reads is empty:
    skip prompt (no signal — pass through)
peak_read = max(substantive_reads)
predicted_message_prefix = max(0, peak_read - 19000)  # subtract tools+system
predicted_rewrite_low  = predicted_message_prefix * 0.85
predicted_rewrite_high = predicted_message_prefix * 1.25
```

Honest about ±25% uncertainty in the prompt: show a range, not a point.

### Pricing

Hardcoded table at `share/ccage-pricing.sh`. Per million tokens, 200K tier (1M tier costs 2× — not handled in v0, document as caveat). Cache-write at 1.25× input.

```
# updated: 2026-05-16
claude-opus-4-7    input=15     cache_write=18.75
claude-sonnet-4-6  input=3      cache_write=3.75
claude-haiku-4-5   input=0.80   cache_write=1.00
```

**Amended 2026-07-20 — the numbers above are wrong and the shape that produced
them was the cause.** Opus is $5/$25, not $15/$75; Haiku 4.5 is $1/$5. Keeping a
separate hand-maintained column per cache tier is what let them drift apart, so
the implementation now keeps ONE input/output table per file and *derives* every
cache rate from it: cache-write 1.25× input at the 5-minute TTL and **2× at the
1-hour TTL** (this spec's flat 1.25× was the third error — long sessions cache at
the 1-hour tier), cache-read 0.1× input. The per-TTL token split is read from
`usage.cache_creation.{ephemeral_5m,ephemeral_1h}_input_tokens` rather than
assumed. The live table lives in `share/ccage-handoff.sh`; the standalone
`share/ccage-pricing.sh` this section describes was never split out.

Computed cost displayed using model from last assistant record's `.message.model`. If unknown, show both Opus and Sonnet estimates.

### Threshold — dollar-denominated

```
CCAGE_RESUME_PROMPT_MIN_USD=0.25    # default; skip prompt below this
```

Compute back to tokens using detected model rate. Below threshold, skip prompt and pass through silently — the cost isn't worth the friction.

### Prompt UX

```
ccage: Session 4f616b4b · 47 turns · last activity 4h 12m ago · claude-opus-4-7
       Resuming will rewrite ~70K tokens (message prefix). Estimated cost: $1.10–$1.65.
       (Claude Code's resume always misses cache; not a TTL issue.)
       [r]esume / [h]andoff / [c]ancel?
```

Implementation:

```bash
_ccage_resume_prompt() {
  local response
  read -n 1 -s response < /dev/tty
  echo "$response"
  _ccage_resume_decide "$response"
}

_ccage_resume_decide() {
  case "$1" in
    r|R|"") echo resume ;;
    h|H)    echo handoff ;;
    *)      echo cancel ;;
  esac
}
```

`_ccage_resume_decide` is a pure function — straightforward to unit-test. The `read` wrapper is integration-tested via stub TTY.

### Branch actions

| Decision | Action |
|---|---|
| `resume` | Return 0 from interceptor; `claude()` proceeds normally. |
| `handoff` | Run `ccage handoff <session>`; print path + paste hint; return 1 (cancel claude invocation). |
| `cancel` | Print `cancelled` to stderr; return 1. |

### Env-var contract

| Var | Default | Effect |
|---|---|---|
| `CCAGE_NO_RESUME_PROMPT` | unset | If `1`, skip interception entirely. |
| `CCAGE_RESUME_PROMPT_MIN_USD` | `0.25` | Skip prompt below this estimated cost. |
| `CCAGE_RESUME_PROMPT_MODEL` | auto | Override model used for cost display. |

All reads use `${var:-default}` for `set -u` safety. Folded with the existing `set -u` fix (see § Phase 6b commit plan).

### Edge cases

- No JSONL in expected location → pass through (claude will error naturally).
- `-r -p` / `--resume --print` (headless) → cannot prompt; pass through with one-line stderr warning.
- `[ ! -t 0 ]` (stdin not a tty) → pass through silently.
- `CCAGE_DISABLE=1` → skip interception (existing ccage opt-out semantics).
- Session id arg matches multiple sessions → pass through with warning (don't guess).

---

## Pricing data freshness

Pricing rot is inevitable. v0 mitigation:
- `share/ccage-pricing.sh` has a `# updated: YYYY-MM-DD` header.
- CHANGELOG entry on every rate refresh.
- `ccage doctor` (future phase) will warn if `pricing.sh` is >180 days old.
- No runtime fetch — installer-time only — to keep ccage offline-clean.

---

## Cache-fix sentinel (post-v0, deferred)

If Anthropic ever fixes the structural `-r` cache miss (issues #42309 / #43657 are still open as of 2026-05-16), the resume-cost prompt becomes noise.

Future `ccage doctor` should include a sentinel: run a 2-message session, `/clear`, `-r`, inspect the next assistant turn's `cache_creation_input_tokens`. If it's near-zero, print: `Claude Code resume cache appears fixed; consider \`export CCAGE_NO_RESUME_PROMPT=1\`.`

Not auto-disabling — let the user decide.

Out of scope for Phase 6a/6b. Track as Phase 6+.

---

## Dependencies

- **`jq`** — new hard dependency. `install.sh` must check `command -v jq`; fail with install hint (`apt install jq`, `brew install jq`, etc.). Add to README "Requirements." `ccage doctor` (future) verifies.
- All other deps already in ccage's existing set: bash≥4 or zsh≥5, coreutils, `stat`, `sed`.

---

## Phase plan

### Phase 6a — `ccage handoff`

**Goal:** offline brief generation from a session JSONL. No API calls.

1. **Tests first** — `tests/test_handoff.bats` + fixture JSONLs in `tests/fixtures/sessions/`:
   - Extract user prompts; exclude tool results, meta messages, system reminders.
   - Files-touched aggregation with correct counts per op.
   - Bash command dedup + trivial filter.
   - Last assistant text isolation + truncation rule.
   - Token totals sum correctly.
   - Malformed-line resilience (warning count, no crash).
   - No-session error path.
   - Prefix-match ambiguity error.
   - Truncation: max-prompts elision marker present and accurate.
2. Implement `share/ccage-handoff.sh`.
3. Implement `bin/ccage` dispatcher.
4. Update `install.sh`:
   - jq dependency check.
   - Drop `bin/ccage` into `$PREFIX/bin/`.
   - Verify on PATH; warn if not.
5. Update `tests/validate-e2e.sh`: run `ccage handoff` against a real ccage-produced session, assert brief structure.
6. Docs:
   - New section in `docs/FEATURES.md` ("Handoff workflow").
   - README subsection ("Avoiding `-r`'s cache rewrite cost").
   - CHANGELOG Unreleased.
7. `simplify` → Opus review → commit.

**Acceptance:** tests green; brief generated from `/home/ff235/dev/ccage` session is human-readable; jq dep failure produces clean error from `install.sh`.

### Phase 6b — `-r` / `-c` interception

**Goal:** intercept resume invocations and surface cost; bundle `set -u` bug fix.

1. **Separate commit first: `fix: ${var:-} defaults for set -u safety`** — touches `share/claude-isolation.sh`'s existing 4 env-var reads (`CCAGE_DISABLE`, `CCAGE_KEEP_ATTRIBUTION`, `CCAGE_KEEP_AUTOUPDATER`, `CCAGE_NO_ONBOARDING_PATCH`, `CCAGE_NO_AUTO_SIGNORE`). Add test in `test_set_u_safety.bats`.
2. **Tests first** — `tests/test_resume_interception.bats`:
   - Detection: `-r <uuid>`, `--resume <uuid>`, `-c`, `--continue` → intercept; bare `-r`, bare `--resume` → pass through.
   - `CCAGE_DISABLE=1` → skip.
   - `CCAGE_NO_RESUME_PROMPT=1` → skip.
   - `[ ! -t 0 ]` → skip.
   - `_ccage_resume_decide` pure-function table: `r`/`R`/empty → resume, `h`/`H` → handoff, anything else → cancel.
   - Cost computation: parameterized fixtures (Opus / Sonnet / Haiku, varying session sizes).
   - Threshold gating: below `CCAGE_RESUME_PROMPT_MIN_USD` → pass through.
   - "No substantive cache_read in session" → pass through.
3. Implement `share/ccage-resume.sh` (`_ccage_intercept_resume`, `_ccage_resume_cost`, `_ccage_resume_decide`) sourced by `claude-isolation.sh`.
4. Implement `share/ccage-pricing.sh` (table + lookup function).
5. Wire interceptor into `claude()` between lines 197 and 204.
6. Update `tests/validate-e2e.sh`: stub `claude` with a fake binary; assert non-tty stdin produces pass-through; assert `CCAGE_NO_RESUME_PROMPT=1` produces pass-through.
7. Docs:
   - README "Resume cost prompt" subsection.
   - FEATURES.md env-var table additions.
   - CHANGELOG Unreleased.
8. `simplify` → Opus review → commit.

**Acceptance:** tests green; manual: `claude -c` on a real stale session shows prompt with sensible numbers; `r` resumes; `h` produces a brief; `c` cancels.

### Phase 6c — Real-world validation + tiered review

**Goal:** prove the brief works as a context bootstrap, not just as a parser exercise.

1. Manual validation:
   - Pick a real ≥30-turn session in this repo.
   - Run `ccage handoff <session>`.
   - Start fresh `claude` session, paste brief, give one-sentence directive: "continue what was happening."
   - Evaluate whether the resumed agent has enough to proceed without further explanation.
   - Document outcome in CHANGELOG Unreleased.
2. Tiered review per `tiered-review` skill:
   - Likely Tier 2 (5–15 commits across 6a + 6b): `simplify` + `find-bugs` + `/second-opinion` + `fp-check`.
   - Promote to Tier 3 if commit count exceeds 15 or if either feature touches the public install surface in a non-trivial way.
3. If review surfaces issues, return to 6a/6b for fixes.

**Acceptance:** real bootstrap works; tiered-review passes.

---

## Risks & mitigations

| Risk | Mitigation |
|---|---|
| JSONL schema changes (Anthropic renames fields) | Tests pinned to fixtures; failures are loud, not silent. Document as "best-effort, schema-dependent" in README. |
| Pricing rates drift | `# updated:` header in `ccage-pricing.sh`; CHANGELOG on refresh; future `ccage doctor` age check. |
| `-r` detection false positives | Whole-word flag match only (`-r`, `--resume`, `-c`, `--continue`); session-id distinguished by UUID-prefix regex. Tests cover edge cases. |
| Cost estimate accuracy | Show as range with ±25% uncertainty; copy explicitly states "estimate"; user can disable prompt. |
| Anthropic fixes structural resume cache miss | Prompt becomes noise; doc the `CCAGE_NO_RESUME_PROMPT=1` opt-out; defer sentinel check to Phase 6+. |
| Brief paste itself uses context tokens | Truncation defaults (20 prompts, 30 files, 40 commands, 600-word assistant tail); honest about diminishing returns for huge sessions. |
| jq dep missing on minimal systems | Install-time check with clear hint; document in README Requirements. |

---

## Decisions log

What was considered and rejected during spec design:

- **LLM-summary handoff via `claude -p`** — rejected because the user clarified they want no separate Claude Code invocations beyond what's in their main session flow. Free jq extraction is strictly within the constraint.
- **Anthropic API calls (Haiku) for cheap summarization** — rejected: separate API billing, violates the no-API-call constraint.
- **`--json` output flag** — rejected as YAGNI; add when a consumer exists.
- **Auto-paste / auto-feed brief into fresh session** — rejected as too magical and fragile; user controls the paste.
- **Background daemon for idle detection + keep-alive ping** — rejected: operational complexity, cost math marginal, most idle sessions never get returned to.
- **Single fixed cost in prompt vs range** — rejected; empirical error is ±20–25%, so a range is honest and a point estimate is misleading.
- **Intercepting bare `claude -r`** — rejected because Claude Code may show its own picker; pass through.
- **Token-denominated threshold** — rejected; cost-of-noticing is dollar-shaped, not token-shaped.
- **Bundling `set -u` fix into a feature commit** — rejected for git-hygiene reasons; keep as separate commit within Phase 6b.
- **Git context in handoff brief** — rejected. ccage must not assume git usage. Users may work in non-git projects (sandboxes, scratch dirs, non-VCS workflows). The brief already lists files-touched from tool_use records; users wanting git state can run `git status` themselves in the new session. No `--with-git-context` flag at any phase.
- **Per-project handoff retention** (`$CLAUDE_CONFIG_DIR/handoffs/`) — rejected. Global flat layout at `~/.local/share/ccage/handoffs/` enables cross-project listing and uniform `ccage prune` semantics. Filenames embed project slug for disambiguation.
- **Stdout-only handoff default** — rejected. The `-r` interception's `[h]andoff` branch needs a file path to print, so a temp file would be required anyway. Default to writing a real file at the canonical location.
- **OSC 52 terminal-escape clipboard fallback** — rejected for v0. Real SSH/remote utility, but some terminals (legacy iTerm modes, certain `screen` configs) break on the escape. Revisit if remote-session usage materializes.
- **Differentiated `-c` vs `-r <id>` prompt copies** — rejected. Single template with verb swap (`Continuing most-recent session` vs `Resuming session`); identical otherwise. Same information either way.

---

## Open questions

None outstanding for spec acceptance. Remaining unknowns surface during implementation:

- Exact threshold tuning (`CCAGE_RESUME_PROMPT_MIN_USD=0.25` is a guess; refine after 6c validation if it's noisy or too quiet).
- Whether brief truncation defaults (20 prompts / 30 files / 40 commands / 600-word assistant tail) feel right in practice. Tune in 6c.
- Whether the spec's predicted-rewrite ±25% band lines up with reality across more session types than the two we sampled. Confirm with at least one large-session test in 6c.

---

## Cross-references

- Build plan: [docs/PLAN.md](PLAN.md)
- Architecture: [docs/ARCHITECTURE.md](ARCHITECTURE.md)
- Features doc: [docs/FEATURES.md](FEATURES.md)
- Resume pointer: [RESUME.md](../RESUME.md) — update Status block + Next action when 6a opens.
